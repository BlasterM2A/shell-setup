.superpowers/

# OS cruft
.DS_Store
Thumbs.db

# Editor/IDE
*.swp
*.swo
*~
.vscode/
.idea/

# Local backups created by fetch_dotfile (never expected inside the repo,
# but ignored defensively in case install.sh is ever run from here)
*.bak.*

# antidote's static plugin bundle cache, auto-generated next to
# zsh/plugins.txt on first `antidote load` — machine-specific absolute
# paths into ~/.cache/antidote, must not be committed
zsh/plugins.zsh

# Local Claude Code session/skill settings — personal to this machine,
# not part of the shell-setup tool itself.
.claude/
